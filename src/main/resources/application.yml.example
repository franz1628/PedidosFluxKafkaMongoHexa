spring:
  application:
    name: hexagonal-demo
  data:
    mongodb:
      uri: mongodb

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

server:
  port: 8080
